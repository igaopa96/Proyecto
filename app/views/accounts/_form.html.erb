<%= form_with(model: account, local: true) do |form| %>
  <% if account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(account.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
        <% account.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_field :descripcion %>
  </div>

  <div class="field">
    <%= form.label 'Tu cuenta es familiar o particular' %>
    <%= form.select(:tipo_familiar, [['Selecciona  ', nil], ['Particular', 1], ['Familiar', 2]]) %>
  </div>
  
  <div class="field">
      <%= form.label 'Que tipo de cuenta es: ' %>
  <%= form.select(:tipo_cuenta, [['Selecciona ', nil], ['Credito', 1], ['Debito', 2], ['Efectivo', 3]]) %>
  </div>

  <div class="field">
    <%= form.label :saldo_inicial %>
    <%= form.text_field :saldo %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
