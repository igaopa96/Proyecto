<%= form_with(model: movement, local: true) do |form| %>
  <% if movement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(movement.errors.count, "error") %> prohibited this movement from being saved:</h2>

      <ul>
        <% movement.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
      <%= form.label 'Concepto'%>
      <%= form.collection_select(:concept_id, @conceptos, :id, :nombre, prompt: true) %>
  </div>

  <div class="field">
      <%= form.label 'Cuenta' %>
      <%= form.collection_select(:account_id, @cuenta, :id, :nombre, prompt: true) %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_field :descripcion %>
  </div>

  <div class="field">
    <%= form.label :cantidad %>
    <%= form.text_field :cantidad %>
  </div>

   <div class="field">
      <%= form.label 'Metas'%>
      <%= form.collection_select(:goal_id, @goals, :id, :nombre, prompt: true) %>
  </div><br>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
